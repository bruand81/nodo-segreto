import 'package:flutter/material.dart';

/// Palette ufficiale AGESCI (colori di branca + istituzionali), coerente
/// con l'icona dell'app (lucchetto + nodo in verde/giallo oro).
class ScoutColors {
  const ScoutColors._();

  /// Viola — Pantone 527C.
  static const Color viola = Color(0xFF7A1E99);

  /// Viola scuro — Pantone 072C.
  static const Color violaScuro = Color(0xFF622599);

  /// Giallo oro — Pantone 123C.
  static const Color gialloOro = Color(0xFFFFCC1E);

  /// Azzurro — Pantone 279C.
  static const Color azzurro = Color(0xFF6689CC);

  /// Giallo L/C — Pantone 109C.
  static const Color gialloLC = Color(0xFFF9D616);

  /// Verde E/G — Pantone 363C.
  static const Color verdeEG = Color(0xFF3D8E33);

  /// Rosso R/S — Pantone 032C.
  static const Color rossoRS = Color(0xFFEF3340);

  /// Sfondo neutro chiaro.
  static const Color parchment = Color(0xFFF6F1E7);
}
